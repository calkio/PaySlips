<UserControl x:Class="PaySlips.UI.View.Workspace.Archive.ArchiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PaySlips.UI.View.Workspace.Archive"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1531">
    <Grid>
        <Grid Margin="30,30,30,759">
            
            <ContentPresenter Content="{Binding Search}"/>
            <ContentPresenter Content="{Binding DirectoryVM}"/>
        </Grid>
        <!--<Label Content="Archive" Margin="0,0,714,415" />-->
    </Grid>
</UserControl>
