<UserControl x:Class="PaySlips.UI.View.Workspace.Archive.ArchiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PaySlips.UI.View.Workspace.Archive"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1531">
    <Grid>
        <Grid Margin="30,30,30,250">

            <ContentPresenter Content="{Binding Search}" Margin="430,10,429,0"/>
            <ContentPresenter Content="{Binding DirectoryVM}" Margin="10,10,726,0"/>
            <ContentPresenter Content="{Binding TitleButtonVM}" Margin="670,10,66,10"/>
            <ContentPresenter Content="{Binding DateButtonVM}" Margin="877,0,-141,10"/>
            <ContentPresenter Content="{Binding FileButtonArchiveVM}" Margin="471,0,429,512"/>
            <ContentPresenter Content="{Binding DeleteButtonVM}" Margin="0,0,806,405"/>
            <ContentPresenter Content="{Binding DeleteAllButtonVM}" Margin="806,10,0,395"/>
            <ContentPresenter Content="{Binding DownloadButtonVM}" Margin="424,144,382,261"/>
            <ContentPresenter Content="{Binding RecoverButtonVM}" Margin="0,144,806,261"/>
        </Grid>
    </Grid>
</UserControl>
