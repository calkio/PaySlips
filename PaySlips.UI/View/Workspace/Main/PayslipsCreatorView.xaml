<UserControl x:Class="PaySlips.UI.View.Workspace.Main.PayslipsCreatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PaySlips.UI.View.Workspace.Main"
             mc:Ignorable="d" 
             
             xmlns:viewWM="clr-namespace:PaySlips.UI.View.Workspace.Main"
             xmlns:vmWM="clr-namespace:PaySlips.UI.ViewModel.Workspace.Main"

             d:DesignHeight="1011" d:DesignWidth="896">

    <UserControl.Resources>
        
        <DataTemplate DataType="{x:Type vmWM:DownloadCreatesVM}">
            <viewWM:DownloadCreatesVIew/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vmWM:CreatorVM}">
            <viewWM:CreatorView/>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.402*"/>
            <RowDefinition Height="0.056*"/>
            <RowDefinition Height="0.088*"/>
            <RowDefinition Height="0.047*"/>
            <RowDefinition Height="0.405*"/>
        </Grid.RowDefinitions>

        <ContentPresenter 
                Grid.Row="0" Grid.RowSpan="3"
                Content="{Binding DownloadCreatesVM}"/>
        
        <ContentPresenter 
                Grid.Row="2" Grid.RowSpan="3"
                Content="{Binding CreatorVM}"/>
    </Grid>
</UserControl>
