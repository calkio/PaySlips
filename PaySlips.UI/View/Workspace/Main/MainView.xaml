<UserControl x:Class="PaySlips.UI.View.Workspace.Main.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PaySlips.UI.View.Workspace.Main"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.034*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.0526*"/>
            <ColumnDefinition Width="0.5656*"/>
            <ColumnDefinition Width="0.0526*"/>
            <ColumnDefinition Width="0.308*"/>
            <ColumnDefinition Width="0.0285*"/>
        </Grid.ColumnDefinitions>

        <ContentPresenter Grid.Row="1" Grid.Column="1"
                          Content="{Binding PayslipsCreatorVM}"/>

        <Grid Grid.Row="1" Grid.Column="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.033*"/>
                <RowDefinition Height="0.695*"/>
                <RowDefinition Height="0.031*"/>
                <RowDefinition Height="0.209*"/>
                <RowDefinition Height="0.03*"/>
            </Grid.RowDefinitions>
            
            <ContentPresenter Grid.Row="1"
                Content="{Binding LastPayslips}"/>

            <DockPanel Grid.Row="3">
                <ContentPresenter DockPanel.Dock="Left"
                    Content="{Binding UnallocatedHours}"/>
                <ContentPresenter DockPanel.Dock="Right"
                                  Margin="20,30,0,0"
                                  Content="{Binding Trash}"/>
            </DockPanel>
        </Grid>
    </Grid>
</UserControl>
