<UserControl x:Class="PaySlips.UI.View.Component.ContainerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PaySlips.UI.View.Component"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border BorderThickness="4" CornerRadius="6" 
                BorderBrush="#005FA8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border BorderThickness="0"  CornerRadius="6,6,0,0" Margin="-2"  
                        BorderBrush="#005FA8" Background="#005FA8">
                    <StackPanel  Grid.Row="0">
                        <Label Content="{Binding Title}" Style="{StaticResource HLabel}"/>
                    </StackPanel>
                </Border>

                <ContentPresenter Content="{Binding SearchLineVM}" 
                                  Grid.Row="1" VerticalAlignment="Center"/>
              
                <ScrollViewer Grid.Row="2" Margin="0" 
                              HorizontalContentAlignment="Center"
                              Style="{StaticResource BeautifulScrollViewerStyle}">
                    <ItemsControl ItemsSource="{Binding Items}" HorizontalAlignment="Stretch">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical" VerticalAlignment="Center"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
