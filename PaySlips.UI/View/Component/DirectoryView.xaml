<UserControl x:Class="PaySlips.UI.View.Component.DirectoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PaySlips.UI.View.Component"
             mc:Ignorable="d">


    <Grid Height="28" Width="255">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="0.4*"/>
            <ColumnDefinition Width="10*"/>
        </Grid.ColumnDefinitions>
        <Border Background="#E0E0E0" Grid.ColumnSpan="3" CornerRadius="4" BorderThickness="0.7" BorderBrush="#005FA8">

        </Border>
        <TextBox Grid.Column="3" 
                 Background="Transparent" 
                 BorderBrush="Transparent" 
                 Style="{StaticResource TextBoxFileDirectoryStyle}" 
                 VerticalAlignment="Center"
                 ></TextBox>
        <Image Source="/Media/img/file_directory.png" VerticalAlignment="Center"/>
        <Image Source="/Media/img/Line_directory.png" Grid.Column="1" Height="20" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
