<UserControl x:Class="PaySlips.UI.View.Component.TrashView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PaySlips.UI.View.Component"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Button Command="{Binding ShowTrash}"
        BorderBrush="Transparent"
        Background="Transparent">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Image x:Name="img"
                               Source="{Binding Source}"
                               Stretch="Uniform"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Right"/>
                                <ControlTemplate.Triggers>
                                    <!-- При наведении меняем Source -->
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="img" Property="Source" Value="{Binding SourceFocus}"/>
                                        <Setter TargetName="img" Property="Margin" Value="-2,0,0,0"/>
                                    </Trigger>
                                    <!-- При нажатии можно вернуть исходный Source, если требуется -->
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="img" Property="Source" Value="{Binding SourceClick}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</UserControl>
