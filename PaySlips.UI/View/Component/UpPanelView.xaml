<UserControl x:Class="PaySlips.UI.View.Component.UpPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PaySlips.UI.View.Component"
             mc:Ignorable="d" >
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <ContentPresenter Content="{Binding DirectoryVM}" Grid.Column="0" Margin="1,12"/>
        <ContentPresenter Content="{Binding Search}" Grid.Column="1"/>
        <ContentPresenter Content="{Binding TitleButtonVM}" Grid.Column="2"/>
        <ContentPresenter Content="{Binding DateButtonVM}" Grid.Column="3"/>

    </Grid>
</UserControl>
