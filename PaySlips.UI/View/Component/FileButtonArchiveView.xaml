<UserControl x:Class="PaySlips.UI.View.Component.FileButtonArchiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PaySlips.UI.View.Component"
             mc:Ignorable="d" >
    <Grid>

        <Button Grid.ColumnSpan="2"
            HorizontalContentAlignment="Center"
            Style="{StaticResource SButton}"
                Margin="10">
            <StackPanel Orientation="Horizontal" >
                <Image Source="/Media/img/file_directory.png" Height="87"/>
                <Label VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource LabelStyle}"  Margin="1,0,0,0" Padding="7" Content="{Binding FirstLabel}"/>
            </StackPanel>
        </Button>



    </Grid>
</UserControl>
