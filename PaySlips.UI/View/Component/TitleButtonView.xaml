<UserControl x:Class="PaySlips.UI.View.Component.TitleButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PaySlips.UI.View.Component"
             mc:Ignorable="d" >
    <Grid>

        <Grid Height="40" Width="150">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"/>

                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Border Background="#E0E0E0" Grid.ColumnSpan="3" CornerRadius="4" BorderThickness="1" BorderBrush="Transparent">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="5" Direction="275" Color="Black" Opacity="0.3"/>
                </Border.Effect>
            </Border>
            <Button Grid.ColumnSpan="2"
        HorizontalContentAlignment="Left"
        Style="{StaticResource ButtonFilter}">
                <StackPanel Orientation="Horizontal">
                    <Label VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource LabelStyle}">Название</Label>
                    <Image Source="/Media/img/TitleButton.png" VerticalAlignment="Center" Height="30" Margin="19,0,0,0"/>
                </StackPanel>
            </Button>
        </Grid>


    </Grid>
</UserControl>
