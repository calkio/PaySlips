<UserControl x:Class="PaySlips.UI.View.Menu.Base.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PaySlips.UI.View.Menu.Base"
             mc:Ignorable="d" Background="Transparent"  BorderBrush="Transparent" BorderThickness="0" 
             d:DesignHeight="1025" d:DesignWidth="339">
    <Border CornerRadius="8">
        <Grid Background="#005FA8">
            <!-- Само меню -->
            <Border BorderThickness="0" CornerRadius="8">
                <ListBox ItemsSource="{Binding MenuItems}"
                         SelectedItem="{Binding SelectedMenuItem}"
                         ItemContainerStyle="{StaticResource SideMenuItemStyle}"
                         BorderThickness="0"
                         Margin="0,20,-10,8"
                         BorderBrush="Transparent" 
                         Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <!-- Шаблон вывода текста внутри пункта -->
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Title}" 
                       FontSize="35" 
                       VerticalAlignment="Center"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <Image Source="{Binding Source}"
                       Stretch="Uniform"
                       Height="Auto"
                       VerticalAlignment="Bottom"
                       Margin="40"
                       HorizontalAlignment="Center"/>
        </Grid>
    </Border>
</UserControl>
